server {
    listen 8080;
    server_name localhost;
    location /stub_status {
        stub_status on;
        access_log off;
        allow 127.0.0.1;
        allow 172.16.0.0/12; # Permite acceso desde redes internas de Docker
        deny all;
    }
}
